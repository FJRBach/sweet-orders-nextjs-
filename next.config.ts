// next.config.ts

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Añade esta línea para resolver el warning
  experimental: {
    // Puedes agregar aquí otras opciones experimentales válidas si lo necesitas
  },
};

export default nextConfig;